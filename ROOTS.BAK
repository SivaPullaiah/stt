	/* Write a C program toe find the roots of a quadratic equation. */

#include<stdio.h>
#include<conio.h>
#include<math.h>

void main()
{
int img,img1,D,a,b,c,root1,root2;
clrscr();
printf("\n Enter values of a,b,c for finding roots of a quadratic eq:\n");
scanf("%f%f%f",&a,&b,&c);

/*checking condition*/
 D=b*b>4*a*c;
if(D==0)
{
	root1=-b/(2*a);
	root2=root1;
	 printf("\n*****ROOTS ARE*****\n");
	printf("\n root1=%d\n root2=%d",root1,root2);
}
else if(D>0)
{
 root1=-b+sqrt(D)/(2*a);
 root2=-b-sqrt(D)/(2*a);
 printf("\n*****ROOTS ARE*****\n");
 printf("\n root1=%d\n root2=%d",root1,root2);
}

else
{
 printf("\n Imaginary Roots.");
img = -sqrt(D)/(2*a);
img1 = -b/(2*a);
 printf("\n*****ROOTS ARE*****\n");
 printf("\n root1=%d + i (%d)\n root2=%d - i(%d)", img1,img,img1,img);

}
 getch();
}
